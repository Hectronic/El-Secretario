[pytest]
testpaths = tests
addopts = -ra
markers =
    ui: tests that exercise Qt UI components
    stress: stress/load-oriented tests
    audio_hw: tests that require real audio input hardware
    gpu: tests that require GPU availability
