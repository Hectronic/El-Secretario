# Implementation Plan: Settings, Secrets, Prompts, Theme, and RAG Runtime

**Branch**: `013-settings-runtime` | **Status**: Implemented

Settings panels and provider configuration are focused under `src/ui/settings/` and `src/ai_providers/`, retaining compatible application imports.
