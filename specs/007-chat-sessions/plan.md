# Implementation Plan: Chat Sessions and Floating Chat

**Branch**: `007-chat-sessions` | **Status**: Implemented

Chat state, runtime, rendering, sessions, floating lifecycle, and standalone compatibility are split under `src/ui/chat/` and main-window coordinators.
