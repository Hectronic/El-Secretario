# Implementation Plan: Summaries and Queueing

**Branch**: `011-summaries-queue` | **Status**: Implemented

Summary generation lives in `src/app/summaries/`; queue lifecycle in `src/app/summary_queue/`; Qt adapters remain compatible façades.
